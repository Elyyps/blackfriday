import React from "react";
import { shallow } from "enzyme";
import { FeaturedDealsComponent } from "./featured-deals.component";
import { getFeaturedDealsDummyData } from "@app/api/modules/featured-deals/endpoints";

describe("[FeaturedShops]", () => {
  // it("should render component without crashing", () => {
  //   const renderedComponent = shallow(<FeaturedDealsComponent {...getFeaturedDealsDummyData()} />);
  //   expect(renderedComponent).toMatchSnapshot();
  // });
});
